const products = ["name", "description", "price", "currency", "categories"];
const users = ["username", "telephone", "password", "email"];
const categories = ["name", "description"];

module.exports = {
  products,
  users,
  categories
};
